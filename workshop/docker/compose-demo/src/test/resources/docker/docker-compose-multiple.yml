---
version: '3'

services:
    # redis 가 제대로 실행되지 않는다. (colima 때문인지?, docker-compose 때문인지?)
    #    redis:
    #        image: redis:7.2
    mongo:
        image: mongo:7

    elasticsearch:
        image: docker.elastic.co/elasticsearch/elasticsearch:8.11.1
        environment:
            - discovery.type=single-node
